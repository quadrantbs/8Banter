<!DOCTYPE html>
<html lang="id">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.3.1/dist/css/bootstrap.min.css">
  <title>Profile Page</title>
  <style>
    .profile-picture {
      width: 150px; /* Atur lebar gambar sesuai keinginan */
      height: auto;
    }
    .center-content {
      text-align: center; /* Untuk teks dan elemen di dalamnya */
    }
    .card-body {
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
    }
  </style>
</head>

<body>
  <header>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
      <a class="navbar-brand" href="/">Logo</a>
      <div class="collapse navbar-collapse">
        <ul class="navbar-nav ml-auto">
          <li class="nav-item">
            <a class="nav-link" href="/">Home</a>
          </li>
        </ul>
      </div>
    </nav>
  </header>

  <div class="container mt-5">
    <div class="text-center mb-4">
      <h1>Profile</h1>
    </div>
    <div class="card mx-auto" style="max-width: 600px;">
      <div class="card-header text-center">
        <h5 class="card-title">User Profile</h5>
      </div>
      <div class="card-body">
        <img src="<%= user.Bio ? user.Bio.profilePicture : '/images/default.png' %>" alt="Profile Picture" class="img-fluid profile-picture">
        <h5>Name: <%= user.name %></h5>
        <p>Email: <%= user.email %></p>
        <p>Biodata: <%= user.Bio ? user.Bio.biodata : 'Kosong' %></p>

        <div class="d-flex justify-content-center mt-3">
          <a href="/users/profile/update/<%= user.id %>" class="btn btn-primary mx-2">Edit Profile</a>
          <form action="/users/profile/<%= user.id %>?_method=DELETE" method="POST" class="d-inline">
            <button type="submit" class="btn btn-danger mx-2">Delete Profile</button>
          </form>
        </div>
      </div>
    </div>
  </div>

  <footer class="bg-dark text-white text-center py-3 mt-5">
    <p>&copy; 2024 Nama Situs. Semua hak dilindungi.</p>
  </footer>
</body>

</html>
